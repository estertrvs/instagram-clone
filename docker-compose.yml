services:
  backend:
    stdin_open: true
    tty: true
    command: tail -f /dev/null
    build:
      context: ./backend/instagram # apontando para a pasta que contém o código
    container_name: instagram-clone-backend
    ports:
      - "8080:8080"
    volumes:
      - ./backend/instagram/:/home/instagram

  frontend:
    stdin_open: true
    tty: true
    build:
      context: ./frontend # apontando para a pasta que contém package.json
    container_name: instagram-clone-frontend
    ports:
      - "3000:3000"
    volumes:
      - ./frontend/instagram/:/home/instagram
      - /home/instagram/node_modules # preserva os node_modules instalados no container

  db:
    image: mariadb:lts
    container_name: instagram-clone-db
    environment:
      - MARIADB_ROOT_PASSWORD=root_password
      - MARIADB_DATABASE=instagram_db
      - MARIADB_USER=instagram_user
      - MARIADB_PASSWORD=instagram_password
    ports:
      - "3306:3306"
    volumes:
      - mariadb_data:/var/lib/mysql

volumes:
  mariadb_data:
    driver: local
